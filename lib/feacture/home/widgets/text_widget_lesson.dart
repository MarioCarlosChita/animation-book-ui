import 'package:flutter/material.dart';

class TextWidgetLesson extends StatelessWidget {
  // final AnimationController animationController ;
  // final
  const TextWidgetLesson({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
    );
  }
}
